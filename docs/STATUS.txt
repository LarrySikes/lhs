LHS STATUS — v0.6 (2026-09-22)
================================

WHAT THIS IS
  LHS = general-purpose language (ease + native code + safety story).
  CLI = lhsc. Sources = *.lhs. Repo = ~/src/newproj.
  Separate from IWAI/UCC (~/src/iwai_project). See HOW_WE_GOT_HERE.txt.

DONE
  - Full toolchain: check / run / watch / fmt / test / build / lib
  - Interpreter + embed natives + Cranelift JIT/AOT
  - Parallel task/await, extern "C", structs/methods, file I/O
  - Shared lhs_mem RC heap + lhs_gc
  - Extended stdlib builtins + `use` module imports
  - stdlib/math.lhs, stdlib/io.lhs; example 17
  - Package search via LHS_PATH; docs/PACKAGES.md
  - Examples 01–17, day planner, CI, licenses

VERIFY
  cd ~/src/newproj
  cargo run -p npc --bin lhsc -- test examples
  cargo run -p npc --bin lhsc -- lib
  cargo run -p npc --bin lhsc -- run --jit examples/17_use_math.lhs

OPTIONAL LATER
  - Remote package registry
  - Namespaced imports (mod::fn) instead of flat merge
  - else-if sugar; richer libc surface

NEXT PRODUCT FORK
  A) Keep polishing LHS
  B) Return to IWAI/UCC
