LHS STATUS — v0.5 (2026-09-21)
================================

WHAT THIS IS
  LHS = general-purpose language (ease + native code + safety story).
  CLI = lhsc. Sources = *.lhs. Repo = ~/src/newproj.
  Separate from IWAI/UCC (~/src/iwai_project). See HOW_WE_GOT_HERE.txt.

DONE
  - Full toolchain: check / run / watch / fmt / test / build
  - Interpreter + embed natives + Cranelift JIT/AOT
  - Parallel task/await, extern "C", structs/methods, file I/O
  - Shared lhs_mem with reference-counted cells/strings + lhs_gc
  - Extended stdlib: getenv, argc/arg, exit, sleep_ms, now_ms, eprint, gc
  - Package layout reserved (stdlib/, docs/PACKAGES.md)
  - Examples 01–16, day planner, CI, licenses

VERIFY
  cd ~/src/newproj
  cargo test --workspace
  cargo run -p npc --bin lhsc -- test examples
  cargo run -p npc --bin lhsc -- run --jit examples/16_stdlib_ext.lhs

OPTIONAL LATER (product polish, not open checklist)
  - Package imports (`use`) + registry
  - Mark-sweep compaction / longer-lived heaps across runs
  - Broader libc bindings
  - Real-world production hardening

NEXT PRODUCT FORK
  A) Keep polishing LHS
  B) Return to IWAI/UCC
